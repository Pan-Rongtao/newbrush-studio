<Controls:MetroWindow x:Class="studio.MainWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
                      xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                      Title="newbrush studio"
                      MinWidth="200"
                      MinHeight="200"
                      Width="1440"
                      Height="720"
                      WindowStartupLocation="CenterScreen" 
                      GlowBrush="{DynamicResource AccentColorBrush}"
    >

    <Window.Resources>
        <Style x:Key="tabItemStyle" TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Button Style="{StaticResource MaterialDesignRaisedAccentButton}" ToolTip="Resource name: MaterialDesignRaisedAccentButton">
                            vertext shader
                        </Button>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
                      
  <Controls:MetroWindow.RightWindowCommands>
    <Controls:WindowCommands>
        <Button Content="settings" />
        <Button>
            <StackPanel Orientation="Horizontal">
                <iconPacks:PackIconModern Width="24" Height="24" Kind="FoodCupcake" />
                <TextBlock Margin="4 0 0 0" VerticalAlignment="Center" Text="deploy cupcakes" />
            </StackPanel>
        </Button>
    </Controls:WindowCommands>
    </Controls:MetroWindow.RightWindowCommands>

    <Grid x:Name="grid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="300"/>
            <ColumnDefinition Width="3" />
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TabControl Margin="10">
            <TabItem Header="vshader" Foreground="Red" >
                <RichTextBox VerticalScrollBarVisibility="Visible" FontSize="20">

                </RichTextBox>
            </TabItem>
            <TabItem Header="fshader" Foreground="DarkBlue" >
                <RichTextBox VerticalScrollBarVisibility="Visible" FontSize="20">

                </RichTextBox>
            </TabItem>
        </TabControl>
        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" />
        <Button Grid.Column="2" Margin="200" Foreground="Blue" FontSize="40" Click="Button_Click">
            Sync
        </Button>
    </Grid>
</Controls:MetroWindow>